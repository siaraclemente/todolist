<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel='stylesheet' href='/stylesheets/style.css' />
  <title>Unit 2 To-Do</title>
  <link href="https://fonts.googleapis.com/css?family=Gloria+Hallelujah" rel="stylesheet">
</head>
<body>
<a href = "/" class = 'Enter'>Leave List</a><br>
<h1>What Needs to Be Done as of <%= time %></h1>
  <ul>
    <% todos.forEach(function(t, idx) { %>
        <a href="/todos/<%= idx %>"><%= t.todo %></a>
          - 
        <%= t.done ? 'Done' : 'Not Done' %>
        <form action="/todos/<%= idx %>?_method=DELETE"
            id="delete-form" method="POST">
            <button type="submit">Delete</button>
          </form>
    <% }); %>
  </ul>
  <a href="/todos/new">Add Item</a>
</body>
</html>